<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.imagespandemo">

    <application
        android:name="com.example.imagespandemo.BaseApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".FansBadgeActivity" />
        <activity android:name=".FansBadgeActivity1" />
        <activity android:name=".PkBadgeActivity" />
        <activity android:name=".fans.ImageSpanActivity" />
        <activity android:name=".span.SpanActivity" />
        <activity android:name=".at.AtActivity"
            android:configChanges="orientation|keyboardHidden"
            android:windowSoftInputMode = "adjustPan"/>
        <activity android:name=".normal_span.NormalSpanActivity" />
        <activity android:name=".line.LineActivity" />
        <activity android:name=".ellipsize.EllipsizeActivity" />
        <activity android:name=".gradient.GradientActivity" />
        <activity android:name=".cache.CacheActivity" />
        <activity android:name=".SpanActivity1" />
        <activity android:name=".shadow.ShadowActivity" />

    </application>

</manifest>